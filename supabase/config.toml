# https://supabase.com/docs/guides/cli/config
# Local project id is filled when you run `supabase link`

[api]
enabled = true

[functions]
# Edge Functions require Supabase Auth JWTs. The mobile app should sign users in with
# Supabase Auth and call functions with the authenticated Supabase client.
verify_jwt = true
